[build]
  command = "hugo"
  publish = "public"

[context.production.environment]
  HUGO_VERSION = "latest"
  HUGO_ENV = "production"

# 构建钩子（可选）
[context.production]
  command = "hugo --minify"

# 开发环境配置
[context.deploy-preview]
  command = "hugo --buildDrafts --buildFuture"
